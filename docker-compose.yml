version: '3.2'

services:
  alpine:
    build:
      context: .
      dockerfile: docker/alpline/Dockerfile
    image: docker.pkg.github.com/hello-world-docker-action/alpline
    cache_from: docker.pkg.github.com/hello-world-docker-action/alpline
  nginx:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    image: docker.pkg.github.com/hello-world-docker-action/nginx
    cache_from: docker.pkg.github.com/hello-world-docker-action/nginx
    ports:
      - "8080:80"
    volumes:
      - ./src:/usr/share/nginx/html